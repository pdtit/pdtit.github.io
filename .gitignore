# Hugo build output (now produced by GitHub Actions, no longer committed)
/docs/
/public/

# Drafts (work-in-progress posts)
/drafts/

# Hugo cache / lock
/resources/_gen/
.hugo_build.lock

# Local Hugo binaries / tooling (installed in CI instead)
hugo.exe
/tools/hugo/
/tools/hugo-*/

# MCP servers with credentials (never commit!)
.vscode/settings.json*
.vscode/mcp.json
tools/mcp-servers/.linkedin-token.json
tools/mcp-servers/node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
social/
